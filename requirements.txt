#--------------------------------------------------------------------------------------------<VQA Late Fusion>-----------||
# Install torch FIRST, separately, with the CUDA build matching your driver :
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Upper bounds are deliberate. transformers 5.x is a major rewrite and peft has known
# issues under it, so this project stays on the 4.x line.

transformers>=4.44,<5.0
tokenizers<0.22
huggingface_hub>=0.23,<1.0
datasets>=2.19,<4.0
peft>=0.11,<0.15
accelerate>=0.30,<2.0

scikit-learn>=1.3
pandas>=2.0
numpy>=1.24,<2.3
matplotlib>=3.7
Pillow>=10.0
tabulate>=0.9

jupyterlab>=4.0
ipywidgets>=8.0
ipykernel>=6.29

# optional dashboard, only if USE_WANDB = True
# wandb>=0.17
